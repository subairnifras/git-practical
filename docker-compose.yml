services:
  app:
    build: .
    container_name: git-practical-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
    mem_limit: 256m
    cpus: 0.5
    command: node src/scripts/nodeCheck.js